const prompt = require('prompt-sync')()

let numero = parseInt(prompt("Digite um número:"))
let fatorial =1

    for (let numero2 = 1; i <= numero; i++) {
        fatorial = fatorial * numero2
    }
    console.log(numero + "! = " + fatorial)

    
